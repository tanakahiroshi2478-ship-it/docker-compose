services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
  client:
    image: alpine
    command: ["sleep", "3600"]
    environment:
      APP_ENV: development

